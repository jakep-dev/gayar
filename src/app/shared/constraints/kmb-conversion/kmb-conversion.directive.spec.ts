import { KmbConversionDirective } from './kmb-conversion.directive';

describe('KmbConversionDirective', () => {
  it('should create an instance', () => {
    const directive = new KmbConversionDirective();
    expect(directive).toBeTruthy();
  });
});
