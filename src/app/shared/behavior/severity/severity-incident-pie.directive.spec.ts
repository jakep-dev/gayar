import { SeverityIncidentPieDirective } from './severity-incident-pie.directive';

describe('SeverityIncidentPieDirective', () => {
  it('should create an instance', () => {
    const directive = new SeverityIncidentPieDirective();
    expect(directive).toBeTruthy();
  });
});
