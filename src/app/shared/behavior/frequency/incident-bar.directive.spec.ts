import { IncidentBarDirective } from './incident-bar.directive';

describe('IncidentDirective', () => {
  it('should create an instance', () => {
    const directive = new IncidentBarDirective();
    expect(directive).toBeTruthy();
  });
});
