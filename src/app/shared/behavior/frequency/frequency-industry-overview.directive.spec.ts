import { FrequencyIndustryOverviewDirective } from './frequency-industry-overview.directive';

describe('FrequencyIndustryOverviewDirective', () => {
  it('should create an instance', () => {
    const directive = new FrequencyIndustryOverviewDirective();
    expect(directive).toBeTruthy();
  });
});
