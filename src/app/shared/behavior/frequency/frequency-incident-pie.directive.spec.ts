import { FrequencyIncidentPieDirective } from './frequency-incident-pie.directive';

describe('FrequencyIncidentPieDirective', () => {
  it('should create an instance', () => {
    const directive = new FrequencyIncidentPieDirective();
    expect(directive).toBeTruthy();
  });
});
