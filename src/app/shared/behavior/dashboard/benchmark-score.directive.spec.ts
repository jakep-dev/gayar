import { BenchmarkScoreDirective } from './benchmark-score.directive';

describe('BenchmarkScoreDirective', () => {
  it('should create an instance', () => {
    const directive = new BenchmarkScoreDirective();
    expect(directive).toBeTruthy();
  });
});
