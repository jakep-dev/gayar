import { KmbConversionPipe } from './kmb-conversion.pipe';

describe('KmbConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new KmbConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
