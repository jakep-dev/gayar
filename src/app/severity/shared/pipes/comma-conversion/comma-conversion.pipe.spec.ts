import { CommaConversionPipe } from './comma-conversion.pipe';

describe('CommaConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new CommaConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
