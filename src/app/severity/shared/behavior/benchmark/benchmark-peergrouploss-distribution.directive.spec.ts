import { BenchmarkPeerGroupLossDistributionDirective } from './benchmark-peergrouploss-distribution.directive';

describe('BenchmarkPeerGroupLossDistributionDirective', () => {
  it('should create an instance', () => {
    const directive = new BenchmarkPeerGroupLossDistributionDirective();
    expect(directive).toBeTruthy();
  });
});
