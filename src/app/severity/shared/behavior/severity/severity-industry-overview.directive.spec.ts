import { SeverityIndustryOverviewDirective } from './severity-industry-overview.directive';

describe('SeverityIndustryOverviewDirective', () => {
  it('should create an instance', () => {
    const directive = new SeverityIndustryOverviewDirective();
    expect(directive).toBeTruthy();
  });
});
