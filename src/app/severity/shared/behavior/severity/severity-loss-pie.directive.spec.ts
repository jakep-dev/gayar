import { SeverityLossPieDirective } from './severity-loss-pie.directive';

describe('SeverityLossPieDirective', () => {
  it('should create an instance', () => {
    const directive = new SeverityLossPieDirective();
    expect(directive).toBeTruthy();
  });
});
